# MongoDB Configuration

# For Docker MongoDB (with authentication)
MONGODB_URI=mongodb://mothrbox_user:mothrbox_password@localhost:27017/mothrbox?authSource=mothrbox

# For Docker MongoDB (without authentication - simpler, less secure)
# MONGODB_URI=mongodb://localhost:27017/mothrbox

# For local MongoDB (no Docker)
# MONGODB_URI=mongodb://localhost:27017

# For MongoDB Atlas (cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/mothrbox

# Database name
DATABASE_NAME=mothrbox

# JWT Secret (CHANGE THIS in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-to-something-random

# Server configuration (optional)
# PORT=3000
# HOST=127.0.0.1
